/**
 * ENVIRONMENT (Produção)
 */
export const environment = {
    production: true,
    // O IP 10.0.2.2 é um alias especial do Emulador Android (Android Studio)
    // para acessar o localhost da máquina hospedeira.
    // O IP onde executa o backend pode ser usado quando estiver executando o App pelo seu device.
    apiUrl: 'https://detailed-krysten-unconformable.ngrok-free.dev'
};
